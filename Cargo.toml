[workspace]
resolver = "2"
members = ["dbe", "node_macro", "eframe_runner", "egui_node_graph/egui_node_graph", "utils", "list_edit"]

# Enable a small amount of optimization in debug mode
[profile.dev]
opt-level = 1

# Enable high optimizations for dependencies (incl. Bevy), but not for our code:
[profile.dev.package."*"]
opt-level = 3
